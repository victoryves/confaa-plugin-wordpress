# ─── Supabase (configurar no Vercel) ───────────────────────────────────────────
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_...

# ─── WordPress ─────────────────────────────────────────────────────────────────
# NÃO precisam ficar aqui. Configuram-se no painel admin do plugin WordPress.
# O plugin envia esses valores no payload a cada requisição ao Vercel.
#
# WP_URL          → "Vercel Endpoint URL" no admin do plugin
# WP_USERNAME     → "WordPress Username" no admin do plugin
# WP_APP_PASSWORD → "Application Password" no admin do plugin
# API_SECRET_KEY  → "API Secret Key" no admin do plugin
